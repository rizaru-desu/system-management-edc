DATABASE_URL=postgres://postgres:postgres@localhost:5432/system_management_edc
BETTER_AUTH_SECRET=replace-with-a-long-random-secret
BETTER_AUTH_URL=http://localhost:3001
# Comma-separated list of origins allowed to send credentialed requests (Web, Expo, Emulators, LAN IP)
# In development, localhost, 127.0.0.1, 10.0.2.2, and current LAN IPv4 are automatically added.
TRUSTED_ORIGINS=http://localhost:5173,http://192.168.1.10:5173,exp://192.168.1.10:8081
# Public origin of the web console; auth emails redirect here after
# verification / password reset. Must be listed in TRUSTED_ORIGINS.
WEB_APP_URL=http://localhost:5173
PORT=3001
LDAP_URL=ldap://ldap.forumsys.com:389
LDAP_BIND_DN=cn=read-only-admin,dc=example,dc=com
LDAP_BIND_PASSWORD=password
LDAP_SEARCH_BASE=dc=example,dc=com
LDAP_USERNAME_ATTRIBUTE=uid
LDAP_EMAIL_DOMAINS=ldap.forumsys.com

# SMTP mail server — defaults suit a local dev catcher (e.g. Mailpit on 1025).
# For a real provider set host/port/credentials, e.g. smtp.gmail.com:465 with SMTP_SECURE=true.
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=System Management EDC <no-reply@example.com>
