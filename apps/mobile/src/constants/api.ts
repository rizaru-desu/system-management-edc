/**
 * @deprecated Prefer importing directly from '@/config/api'
 * Re-exported here for backwards compatibility.
 */
export {
  API_BASE_URL,
  API_CONFIG,
  API_ENDPOINTS,
  DEFAULT_API_PORT,
  ANDROID_EMULATOR_HOST,
  LOCALHOST,
  getApiBaseUrl,
  getMetroHost,
  default,
} from '@/config/api';

